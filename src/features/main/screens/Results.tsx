import { Common } from "../../common";

const {
  Components: { Container },
} = Common;

export const ResultsScreen = () => {
  return (
    <Container>
      <h1>Result Screen</h1>
    </Container>
  );
};
